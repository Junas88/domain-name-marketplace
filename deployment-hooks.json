{
  "deploymentHooks": {
    "preDeploy": [
      {
        "name": "Clear domains on deploy",
        "script": "node deployment-setup.js"
      }
    ],
    "postDeploy": [
      {
        "name": "Verify domain clearance",
        "script": "curl -X GET $REPLIT_DEPLOYMENT_URL/api/domains | grep -q '\\[\\]' && echo 'Domains cleared successfully!' || echo 'WARNING: Domains still exist in production!'"
      }
    ]
  }
}